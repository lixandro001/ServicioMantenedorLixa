using Domain.Enumerations;
using Domain.Payloads.Auths;
using Domain.Results.Client;
using Domain.Results.Users;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace Application.Interfaces.IRepositories
{
   public interface IClientRepository
    {
        //Task<(ServiceStatus, string, UserMeResult)> GetInfoPaient(AuthPayload payload);
        //Task<(ServiceStatus, string, long, List<ClientTicketResult>)> GetInfoPaientTikets(AuthPayload payload, int? page = 1);
        //Task<(ServiceStatus, string, long, List<ClientTicketDetailResult>)> GetInfoPaientTiketsDetail(string numoscab, string peroscab, string anooscab, string numsuc, string numemp, int? page = 1);
    
    }
}
